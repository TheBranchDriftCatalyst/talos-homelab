{"_type":"issue","id":"TALOS-asv","title":"Incident retro: UPS fault → cluster-wide outage + XFS corruption (2026-05-09)","description":"On 2026-05-09 a UPS fault caused all 5 Talos nodes to power off uncleanly. The control plane (talos00) booted back into a stuck \"Booting\" state because EPHEMERAL XFS (/dev/sda4) corrupted during the unclean shutdown — kernel reported \"Metadata has LSN (7413:30756) ahead of current LSN (7413:30281)\", \"log mount/recovery failed: error -117\", and Talos's auto xfs_repair refused with \"valuable metadata changes in a log which needs to be replayed\". Talos has no auto `xfs_repair -L` and no in-cluster shell to run it manually.\n\nNo etcd snapshots existed (see TALOS-a8g), so recovery required a fresh bootstrap with full local-path PVC loss (Prometheus TSDB, Grafana, Graylog, OpenSearch, MongoDB, Nexus, etc.). GitOps (Flux + ArgoCD) reconciled application manifests but state-bearing PVCs were unrecoverable.\n\nKnown Talos issue: GH siderolabs/talos#9217, #8292 — XFS on bare metal Talos breaks on every power loss. Closed as \"not planned\".\n\nThis is a **post-incident analysis** task, not a fix task. Output is a recommendation memo + decision; implementation tickets get filed separately.","acceptance_criteria":"- Document timeline + root cause analysis\n- Evaluate options and pick a path:\n  1. Power resilience: redundant UPS (dual-feed, A/B power), UPS battery health check, NUT/apcupsd shutdown integration to gracefully `talosctl shutdown` before battery dies\n  2. HA control plane: 3-master architecture (etcd quorum survives 1 node loss → no single-point XFS corruption disaster)\n  3. Filesystem: switch EPHEMERAL from XFS to ext4 (Talos config supports this in newer versions; ext4 handles unclean shutdowns better)\n  4. Combination: all of the above\n- Cost/complexity tradeoff documented (extra UPS $, +2 control plane nodes RAM/CPU/electricity, filesystem migration risk)\n- Decision recorded as ADR in docs/02-architecture/\n- Implementation tickets filed for chosen path (one per change)\u003c/acceptance\u003e\n\u003cparameter name=\"design\"\u003eKey questions to answer:\n\n**1. UPS strategy**\n- What UPS do we have now? Single point of failure?\n- NUT integration: Talos doesn't natively run NUT clients, but a sidecar / DaemonSet could call `talosctl shutdown` on low-battery signal\n- Alternative: power monitor on a Pi that triggers shutdown via Talos API\n- Cost: 2nd UPS ~$200-500\n\n**2. 3-master architecture**\n- Currently: 1 control plane (talos00) + 4 workers (talos01, talos02, talos03, talos06)\n- Promoting 2 workers to control plane: lose ~2 nodes worth of workload capacity but gain etcd quorum (can lose 1 of 3 CPs without downtime)\n- talos03 (Ryzen 5800U, idle) is a strong candidate per docs/05-projects/cluster-optimization/\n- talos02 (Intel Ultra 5, 64GB) too capable to \"waste\" on CP role\n- Best mix: talos00 + talos01 + talos03 as CPs\n\n**3. Filesystem swap (XFS → ext4)**\n- Talos machine config: `machine.disks` or `machine.systemDiskEncryption` doesn't directly cover EPHEMERAL FS choice\n- Newer Talos may expose EPHEMERAL filesystem type. Need to verify current support\n- Migration path: each node needs reset --system-labels-to-wipe=EPHEMERAL after config update — sequential, 1 at a time\n\n**4. Combination**\nThe UPS issue and the etcd quorum issue are independent. Power problem → all 5 still drop. So even with 3 CPs, if ALL get power-killed, etcd corrupts on whichever one had ongoing writes.\nTherefore: UPS+graceful-shutdown is the actual defense. HA CP is defense-in-depth for non-power failures (single disk failure, etc.).\n\n**Recommendation (preliminary, validate during ticket work)**: All three. UPS + graceful-shutdown integration is highest leverage. 3-CP is defense in depth. Ext4 reduces blast radius if power-graceful fails.","status":"open","priority":1,"issue_type":"task","owner":"djdanielsh@gmail.com","created_at":"2026-05-09T16:20:45Z","created_by":"DJ Daniels","updated_at":"2026-05-09T16:20:45Z","labels":["disaster-recovery","incident","infrastructure","post-mortem"],"dependencies":[{"issue_id":"TALOS-asv","depends_on_id":"TALOS-a8g","type":"blocks","created_at":"2026-05-09T09:20:45Z","created_by":"DJ Daniels","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"TALOS-a8g","title":"Daily etcd snapshot CronJob with 10-snapshot retention","description":"Automate daily etcd snapshots so future control plane disasters (e.g., XFS EPHEMERAL corruption from power loss like the 2026-05-09 UPS fault) are recoverable without total cluster state loss.\n\nCurrently no etcd snapshots exist. After the UPS fault on 2026-05-09 corrupted the EPHEMERAL XFS partition on talos00, the only recovery path was a fresh bootstrap with full local-path PVC loss (Prometheus TSDB, Grafana, Graylog, OpenSearch, MongoDB, Nexus). Daily snapshots offloaded to durable storage would have allowed `talosctl bootstrap --recover-from=./db.snapshot` instead.\n\nSnapshot creation: `talosctl -n \u0026lt;cp-ip\u0026gt; etcd snapshot \u0026lt;path\u0026gt;` (Talos's preferred consistent snapshot method, vs raw copy from /var/lib/etcd which lives on EPHEMERAL).\n\nReference: docs/02-architecture/dual-gitops.md, Sidero disaster recovery docs.","design":"Approach options to evaluate:\n1. Kubernetes CronJob in cluster running `talosctl etcd snapshot` against the CP node — needs talosconfig secret, kubectl/talosctl image\n2. Host-side cron on a separate machine with talosctl access — survives full cluster loss\n3. Talos built-in scheduled snapshots if available in current version (v1.11.1)\n\nStorage: NFS PVC (media or dedicated backup share). Local-path is unacceptable — defeats the purpose since it dies with EPHEMERAL.\n\nRetention: simple `find -mtime +N -delete` or rclone/restic for offsite copy.\n\nBonus: also snapshot before any planned `talosctl upgrade` or config apply.","acceptance_criteria":"- CronJob runs daily, creates `talosctl etcd snapshot`\n- Snapshots stored on NFS (NOT local-path, which dies with EPHEMERAL)\n- Retention: keep last 10, prune older\n- Alert on snapshot failure (Prometheus/Alertmanager)\n- Documented restore procedure in docs/06-operations/ or similar\n- Verified by manual test restore in non-prod or cluster rebuild","status":"open","priority":1,"issue_type":"feature","owner":"djdanielsh@gmail.com","created_at":"2026-05-09T16:17:47Z","created_by":"DJ Daniels","updated_at":"2026-05-09T16:17:47Z","labels":["backup","disaster-recovery","etcd","talos"],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"TALOS-1e9","title":"Rotate GitHub PAT (leaked in session transcript via curl verbose)","description":"The new GitHub PAT (`ghp_Hl5h...`) was exposed in the 2026-04-11 Claude Code session transcript because the user's shell has curl forced to verbose mode (likely `~/.curlrc` with `-v`), which printed `Authorization: Bearer \u003ctoken\u003e` to stderr when testing ghcr-secret validity.\n\nThe old revoked PAT (`ghp_OwCy...`) was also visible but is already dead (confirmed 401).\n\nAfter rotation:\n1. Create new PAT in GitHub with scopes: `project, repo, workflow, write:packages`\n2. Update 1Password item `github-pat` property `credential` with new value\n3. Force ESO refresh: `kubectl annotate clusterexternalsecret ghcr-secret force-sync=$(date +%s) --overwrite`\n4. Verify: all 6 namespaces pick up new secret within minutes (CES auto-propagates)\n5. Also update Flux git secret: extract new PAT from any ESO-managed ghcr-secret, write to `flux-system/flux-system` secret via temp file (see session transcript for the safe pattern using `--from-file` to avoid process-list exposure)\n6. Silence the verbose curl: check `~/.curlrc` or shell aliases for `-v` flag and remove it\n\nThe ClusterExternalSecret `ghcr-secret` is already deployed and covers: catalyst-data, dungeon-library, ec2-agents, gateway-arr, media-private, scratch.","status":"open","priority":1,"issue_type":"task","owner":"djdanielsh@gmail.com","created_at":"2026-04-12T14:09:47Z","created_by":"DJ Daniels","updated_at":"2026-04-12T14:09:47Z","labels":["credentials","security"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"TALOS-q79","title":"Homepage widget credentials — create 1Password items + regenerate tokens","description":"Several homepage widgets show \"API Error Information\" because the ExternalSecrets reference 1Password items that don't exist yet. The plumbing is in place (ExternalSecrets created in `52c52ac`, env vars on the homepage pod with `optional: true`) — just need the 1Password items created.\n\n## Items to create in 1Password (vault: catalyst-eso)\n\n### 1. qBittorrent web UI credentials\n- **1Password item name:** `qbittorrent-webui`\n- **Fields:** `username` = `panda`, `password` = `turbopookipanda`\n- **Homepage env vars:** `HOMEPAGE_VAR_QBITTORRENT_USER`, `HOMEPAGE_VAR_QBITTORRENT_PASS`\n- **ExternalSecret:** `applications/arr-stack/base/homepage/qbittorrent-externalsecret.yaml`\n\n### 2. Tautulli API key\n- **1Password item name:** `tautulli-api-key`\n- **Fields:** `password` = (get from Tautulli → Settings → Web Interface → API key)\n- **Homepage env var:** `HOMEPAGE_VAR_TAUTULLI_KEY`\n- **ExternalSecret:** `applications/arr-stack/base/homepage/tautulli-externalsecret.yaml`\n\n### 3. Home Assistant long-lived access token\n- **1Password item name:** `homeassistant-token`\n- **Fields:** `password` = (generate in HA → Settings → User → Security → Long-Lived Access Tokens → Create Token)\n- **Homepage env var:** `HOMEPAGE_VAR_HOMEASSISTANT_KEY`\n- **ExternalSecret:** `applications/arr-stack/base/homepage/homeassistant-externalsecret.yaml`\n\n### 4. Authentik API token (regenerate)\n- **1Password item name:** `authentik-api-token` (already exists but token returns 403)\n- **Action:** Generate new API token in Authentik Admin → Directory → Tokens \u0026 App passwords (Intent: API Token). Grant permissions: \"Can view User\" (authentik Core), \"Can view Event\" (authentik Events). Update the 1Password item with the new token.\n- **Homepage env var:** `HOMEPAGE_VAR_AUTHENTIK_KEY`\n\n## After creating items\nESO refreshes hourly. To speed it up:\n```bash\nkubectl annotate externalsecret -n media homepage-qbittorrent-credentials force-sync=$(date +%s) --overwrite\nkubectl annotate externalsecret -n media homepage-tautulli-credentials force-sync=$(date +%s) --overwrite\nkubectl annotate externalsecret -n media homepage-homeassistant-credentials force-sync=$(date +%s) --overwrite\nkubectl annotate externalsecret -n media homepage-authentik-credentials force-sync=$(date +%s) --overwrite\nkubectl delete pod -n media -l app=homepage\n```\n\n## Also investigate\n- **Radarr** widget shows all dashes — may be a transient issue or the API key might need rotation. Check `kubectl logs -n media deploy/homepage | grep -i radarr` after other widgets are fixed.\n- **MinIO** siteMonitor shows Offline — the `/minio/health/live` endpoint may require auth. Try changing to `/minio/health/cluster` or removing siteMonitor.\n- **Tdarr** siteMonitor shows Offline despite correct URL — may be intermittent slow responses exceeding homepage's timeout.","status":"open","priority":2,"issue_type":"task","owner":"djdanielsh@gmail.com","created_at":"2026-04-12T22:46:39Z","created_by":"DJ Daniels","updated_at":"2026-04-12T22:46:39Z","labels":["credentials","homepage"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"TALOS-7a7","title":"Migrate Home Assistant recorder off NFS-backed SQLite","description":"Home Assistant's `/config` volume is NFS-backed (`fatboy-nfs-appdata`, PVC `homeassistant-config-nfs`). The SQLite recorder database (`home-assistant_v2.db`) on NFS causes:\n\n1. **SIGBUS crashes**: `Fatal Python error: Bus error` during SQLAlchemy recorder commits — SQLite's mmap + NFS coherency is a known-bad combo\n2. **Liveness probe kills**: NFS latency spikes stall the asyncio event loop → `GET /` probe times out → kubelet kills pod (exit 137)\n3. **69 restarts** accumulated, most recent 2026-04-08\n\nA probe relaxation was applied as a bandaid (`66be6c8` — loosened liveness from ~150s to ~600s kill window), but the fundamental issue remains: SQLite on NFS is unsafe.\n\nOptions (pick one):\nA) **Migrate recorder to external DB**: Add `recorder.db_url: postgresql://...` to HA's `configuration.yaml`, pointing at a dedicated postgres instance (could reuse cloudnative-pg in the `databases` namespace). This is the HA-recommended approach for anything beyond a single SD card.\nB) **Move /config to local-path PVC**: Preserves SQLite but puts it on local disk. Downside: node-pinned, need Velero backups for persistence across node failures.\nC) **Hybrid**: local-path PVC for the SQLite DB file only (via a separate volume mount at `/config/.storage/`), keep the rest of /config on NFS for cross-node access.\n\nAlso noted:\n- HA container has **no memory limit** (only request 512Mi, currently using ~450Mi). Consider adding `limits.memory: 2Gi` to prevent unbounded growth.\n- HA logs to `/config/home-assistant.log` on disk, not stdout. Add `HASS_LOG_TO_STDOUT=1` for better k8s-native log access.\n\nDeployment file: `applications/home-automation/base/homeassistant/deployment.yaml`\nPVC file: `applications/home-automation/base/homeassistant/pvc.yaml`","status":"closed","priority":2,"issue_type":"task","owner":"djdanielsh@gmail.com","created_at":"2026-04-12T14:10:11Z","created_by":"DJ Daniels","updated_at":"2026-04-12T14:20:58Z","closed_at":"2026-04-12T14:20:58Z","close_reason":"Deployed dedicated postgres:15-alpine in home-automation namespace (postgres.yaml). Configured HA recorder to use postgresql:// via configmap + init-container patch. Verified: 13 recorder tables created in postgres, HA serving web UI, 0 restarts. Tightened liveness probe timings now that SQLite-on-NFS root cause is eliminated. Old SQLite DB remains on NFS as backup. Commit 7f44c81.","labels":["home-automation","stability","storage"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"TALOS-lyg","title":"Apply otel-operator leaderElection.enabled=false to stop restart flapping","description":"The `monitoring/opentelemetry-operator` manager container has accumulated 2070+ restarts because it exits on leader election lease loss (controller-runtime default behavior). Any brief API server network blip (~20s) causes the lease renewal to time out → exit code 1 → kubelet restart. ~34 flaps/day historically.\n\nRoot cause confirmed from previous-container logs showing:\n```\nfailed to renew lease monitoring/9f7554c3.opentelemetry.io: timed out waiting for the condition\n\"problem running manager\", \"error\": \"leader election lost\"\n```\n\nNOT OOM (exit code 1, not OOMKilled). Resources are chart defaults: requests 100m/64Mi, limits 100m/128Mi — adequate.\n\nSince this is a single-replica deployment with no HA coordination needed, disabling leader election eliminates the failure path entirely.\n\nFix: edit `infrastructure/base/monitoring/v2-otel/operators/otel-operator/helmrelease.yaml`, add under `values`:\n```yaml\nmanager:\n  leaderElection:\n    enabled: false\n```\n\nThis will trigger a pod rollout. Apply during a quiet period since the pod is currently stable (last restart 2026-04-08). Upstream reference: open-telemetry/opentelemetry-operator#2460.\n\nCurrent resources (chart defaults):\n- manager: requests cpu=100m mem=64Mi, limits cpu=100m mem=128Mi\n- kube-rbac-proxy: requests cpu=5m mem=64Mi, limits cpu=500m mem=128Mi","status":"closed","priority":2,"issue_type":"task","owner":"djdanielsh@gmail.com","created_at":"2026-04-12T14:09:58Z","created_by":"DJ Daniels","updated_at":"2026-04-12T14:20:00Z","closed_at":"2026-04-12T14:20:00Z","close_reason":"Applied leaderElection.enabled=false in helmrelease.yaml. Pod rolled out with 0 restarts (5 transient restarts during rollout due to ServiceMonitor conflict, resolved by deleting stale ServiceMonitor). New pod running 2/2 on talos06.","labels":["monitoring","stability"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"TALOS-1me","title":"Add bolt entrypoint (7687) to Traefik for Neo4j Browser ingress","description":"Neo4j Browser SPA loads from port 7474 (HTTP) but then connects via WebSocket/Bolt to port 7687 for queries. To support a fully-functional http://neo4j.talos00 ingress, add a 'bolt' port entry to the Traefik HelmRelease so Traefik listens on 7687 with a corresponding hostPort. Companion IngressRouteTCP lives in catalyst-data. Causes one brief (~30s) Traefik pod restart on reconcile due to maxUnavailable=100%, maxSurge=0.","status":"closed","priority":2,"issue_type":"task","owner":"djdanielsh@gmail.com","created_at":"2026-04-11T23:48:20Z","created_by":"DJ Daniels","updated_at":"2026-04-12T04:29:09Z","closed_at":"2026-04-12T04:29:09Z","close_reason":"Shipped in 7ea1a18 (bolt port entry) + user fix 908f516 (updateStrategy top-level). Rollout verified via playwright.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"TALOS-ch7","title":"Migrate catalyst-data + catalyst-ui to ImageUpdater CRD pattern","description":"Replace the annotation-based argocd-image-updater config on catalyst-data and catalyst-ui Applications with the v1.0 ImageUpdater CRD. catalyst-ui already has a CRD (needs annotation cleanup). catalyst-data needs a brand-new CRD covering all 6 services (congress, media, leaks, kg, explorer, kg-api). Also covers the two previously-manual services (knowledge-graph, data-explorer) which had no image-updater coverage at all.","status":"closed","priority":2,"issue_type":"task","owner":"djdanielsh@gmail.com","created_at":"2026-04-11T23:16:13Z","created_by":"DJ Daniels","updated_at":"2026-04-11T23:21:01Z","closed_at":"2026-04-11T23:21:01Z","close_reason":"Shipped in a5355be. Legacy annotations removed from both Applications; new catalyst-data ImageUpdater CRD covers all 6 services.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"TALOS-raf","title":"Wire knowledge-graph-api into argocd-image-updater","description":"Add kg-api=ghcr.io/thebranchdriftcatalyst/knowledge-graph-api to the image-list annotation on the catalyst-data argocd Application, plus update-strategy=latest. Mirrors the existing congress/media/leaks pattern so new :latest pushes from catalyst-data CI auto-roll the pod instead of requiring manual kubectl rollout restart.","status":"closed","priority":2,"issue_type":"task","owner":"djdanielsh@gmail.com","created_at":"2026-04-11T23:02:22Z","created_by":"DJ Daniels","updated_at":"2026-04-11T23:06:52Z","closed_at":"2026-04-11T23:06:52Z","close_reason":"Shipped in a6790a6. Argocd will pick up the annotation on next sync; image-updater will auto-detect new kg-api:latest pushes thereafter.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"TALOS-9lu","title":"Fix catalyst-data PVC accessModes mismatch blocking ArgoCD sync","description":"The `catalyst-data` ArgoCD application is stuck OutOfSync/Degraded because two PVCs cannot be patched to their desired access mode:\n\n- `catalyst-data/metube-downloads`: live = `ReadOnlyMany`, git = `ReadWriteMany`\n- `catalyst-data/tubesync-downloads`: same mismatch\n\nPVC `spec.accessModes` is immutable after creation, so ArgoCD retries patches forever and logs:\n\n    PersistentVolumeClaim \"tubesync-downloads\" is invalid: spec: Forbidden: spec is immutable after creation except resources.requests and volumeAttributesClassName for bound claims\n\nThe underlying PVs (`catalyst-data-metube-downloads`, `catalyst-data-tubesync-downloads`) are NFS-backed, already `ReadWriteMany`, and have `persistentVolumeReclaimPolicy: Retain`, so data is safe to preserve across a PVC delete+recreate.\n\nSource of truth: `catalyst-data/k8s/storage/nfs-media-volumes.yaml` in the `catalyst-data` repo.\n\nDiscovered 2026-04-11 during cluster health pass — not blocking any currently-running workload (dagster media-ingest is using the existing ROX PVCs fine today), but keeps the argo app red and prevents further changes from syncing cleanly.","design":"Delete + recreate cycle, preserving data via Retain PVs:\n\n1. Identify consumers:\n   ```\n   kubectl get pods -n catalyst-data -o json | jq '.items[] | select(.spec.volumes[]?.persistentVolumeClaim.claimName | test(\"metube-downloads|tubesync-downloads\")) | .metadata.name'\n   ```\n   Plus check k8sRunLauncher job template in `dagster-instance.yaml` for any job-level consumers.\n\n2. Scale consuming deployments to 0 (at minimum `media-ingest`).\n\n3. Delete both PVCs:\n   ```\n   kubectl delete pvc -n catalyst-data metube-downloads tubesync-downloads\n   ```\n   PVs will go to `Released`.\n\n4. Clear claimRef so PVs return to `Available`:\n   ```\n   kubectl patch pv catalyst-data-metube-downloads --type=json -p='[{\"op\":\"remove\",\"path\":\"/spec/claimRef\"}]'\n   kubectl patch pv catalyst-data-tubesync-downloads --type=json -p='[{\"op\":\"remove\",\"path\":\"/spec/claimRef\"}]'\n   ```\n\n5. Trigger ArgoCD sync — it will create new PVCs from git, which should bind to the existing PVs via `spec.volumeName`.\n\n6. Verify `kubectl get pvc -n catalyst-data` shows RWX and Bound.\n\n7. Scale consumers back up.\n\nAlternative considered: manual `volumeName`-less PVCs that dynamic-provision fresh NFS volumes — rejected because it would orphan existing data on the NAS.","acceptance_criteria":"- `kubectl get application -n argocd catalyst-data` reports `Synced` and `Healthy`\n- Both PVCs show `RWX` in `kubectl get pvc -n catalyst-data`\n- `media-ingest` and any other consuming pods are Running\n- No data loss on the NFS server (verify `/volume1/appdata/downloads/metube-downloads` and `tubesync-downloads` still contain pre-fix files)","status":"closed","priority":2,"issue_type":"bug","owner":"djdanielsh@gmail.com","created_at":"2026-04-11T19:25:15Z","created_by":"DJ Daniels","updated_at":"2026-04-11T20:54:38Z","closed_at":"2026-04-11T20:54:38Z","close_reason":"Fixed live: scaled down data-explorer + media-ingest, deleted both PVCs (metube-downloads, tubesync-downloads), cleared claimRef on the NFS-backed PVs so they went Available, triggered argocd hard refresh + sync, PVCs were recreated from git with ReadWriteMany and re-bound to the existing Retain PVs (no data loss), scaled consumers back up. catalyst-data argocd app is now Synced. Verified both PVCs show RWX and both pods are Running.","labels":["argocd","catalyst-data","pvc"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"TALOS-2g1","title":"Annotate remaining IngressRoutes for homepage auto-discovery (Phase 4)","description":"Phase 1-3 of the homepage migration to IngressRoute annotation-based discovery is complete. 23 IngressRoutes in talos-homelab are annotated, and the static ConfigMap was stripped to only services that can't yet be auto-discovered.\n\nRemaining static entries that need IngressRoute annotations added in their respective repos:\n\n**catalyst-llm repo** (`workspace/catalyst-llm/`):\n- Open WebUI (`open-webui` IngressRoute)\n- LLM Proxy / LiteLLM (`litellm` IngressRoute)\n- RabbitMQ (`rabbitmq` IngressRoute — if exists)\n\n**talos-homelab** (IngressRoutes exist but managed by Helm/other):\n- Authentik (`infrastructure/base/authentik/` — has widget, needs annotation)\n- Traefik dashboard (`traefik` Helm chart manages this IngressRoute)\n- Nexus (`infrastructure/base/registry/` — check if IngressRoute exists)\n\n**talos-private repo** (`workspace/talos-private/`):\n- SecureChrome, SecureXNG (vpn-gateway IngressRoutes — may be TCP-only)\n\n**Not annotation-compatible** (keep in static config permanently):\n- Flux (Grafana dashboard link, not a real service)\n- Mimir/Loki/Tempo (Grafana explore page links)\n- VPN Gateway (Grafana dashboard link)\n- Bastion (shell pod, no web UI)\n\nOnce all repos are annotated, the `homepage-services` ConfigMap can be reduced to just the permanently-static entries (Flux, Mimir/Loki/Tempo, VPN Gateway, Bastion).\n\nCommits from Phase 1-3:\n- `2d1125c` — RBAC + traefik: true\n- `d776bc4` — infrastructure IngressRoute annotations\n- `94972e1` — media/app IngressRoute annotations\n- `e752ede` — static ConfigMap cleanup","status":"open","priority":3,"issue_type":"task","owner":"djdanielsh@gmail.com","created_at":"2026-04-12T16:19:59Z","created_by":"DJ Daniels","updated_at":"2026-04-12T16:19:59Z","labels":["cleanup","homepage"],"dependency_count":0,"dependent_count":0,"comment_count":0}
