package main

// search holds two sub-units, indexer/ and query/. They are NOT components: components.path is
// services and the glob is one level deep, so `search` is one unit that happens to have
// internal structure. That is the correct reading for a plain codebase.
func main() {}
