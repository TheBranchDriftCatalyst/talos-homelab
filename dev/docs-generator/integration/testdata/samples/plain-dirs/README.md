# Plain Dirs

A repository with no GitOps controller. Services live in [services](services), shared code in
[lib](lib), prose in [notes](notes).

No frontmatter here on purpose: this is the migration worklist entry for this sample.
