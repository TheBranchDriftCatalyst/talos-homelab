apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: telemetry
  namespace: flux-system
spec:
  interval: 10m
  path: ./platform/telemetry
  prune: true
  suspend: true # the only suspended component in this sample
  sourceRef:
    kind: GitRepository
    name: orchard
