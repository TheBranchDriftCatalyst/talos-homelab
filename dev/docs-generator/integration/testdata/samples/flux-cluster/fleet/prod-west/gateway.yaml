apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: gateway-controller # deliberately NOT the filename: slug stays `gateway`
  namespace: flux-system
spec:
  interval: 10m
  path: ./platform/gateway
  prune: true
  sourceRef:
    kind: GitRepository
    name: orchard
