# Deliberately malformed. A linter that dies on one bad manifest is useless in exactly the
# moment you need it, so the run must survive this file.
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: broken
 spec:
    path: ./platform/broken
  interval: "10m
