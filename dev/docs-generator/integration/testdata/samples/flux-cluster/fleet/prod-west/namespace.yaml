# Not a Kustomization. Must be ignored silently — enumerating it as a component would invent a
# deployable unit that Flux never reconciles.
apiVersion: v1
kind: Namespace
metadata:
  name: orchard-system
