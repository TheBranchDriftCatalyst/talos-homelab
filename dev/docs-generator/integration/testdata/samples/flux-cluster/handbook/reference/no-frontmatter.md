# Environment Variables

No frontmatter at all. This is migration worklist entry three of three, and it raises no schema
findings: an absent frontmatter block is the worklist, not an error.

Its links are valid, so it raises nothing anywhere: [the handbook](../README.md).
