---
type: overview
status: current
covers:
  - path:handbook/reference
freshness: follows-code
tickets:
  - ORCH-101
summary: Index of the reference tables, generated from the tree between the markers below.
---

# Reference

EXPECT: none

This file is MARKER-OWNED. Everything outside the two `docs:gen:nav` markers below is prose
docsgen must never touch; everything between them is a table docsgen rewrites from the tree.

The paragraph you are reading is the fixture's whole point: if a regeneration ever changes one
byte of it, marker ownership is broken and the specs that assert byte-identical prose are the
only thing standing between this file and a generator that eats editorial work.

<!-- docs:gen:nav -->

<!-- /docs:gen:nav -->

A second paragraph AFTER the region, because a splice that got its end offset wrong would
swallow this one and the file would still look plausible.

## Tracking

- ORCH-101 — handbook structure
