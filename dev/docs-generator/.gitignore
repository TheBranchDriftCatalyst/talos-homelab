# Build output. The binary is compiled by `task docs:build` and put on PATH by the flake's
# shellHook; it is never committed — a checked-in binary would be stale within a day and would
# also make the module un-portable across architectures.
bin/
