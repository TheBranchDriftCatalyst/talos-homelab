# All three control planes carry this. It keeps them out of external load balancer pools —
# standard practice, since a control plane answering LB traffic competes with the API server.
machine:
  nodeLabels:
    node.kubernetes.io/exclude-from-external-load-balancers: ""
