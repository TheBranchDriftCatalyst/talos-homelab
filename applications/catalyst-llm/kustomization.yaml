---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: catalyst-llm

resources:
  - namespace.yaml
  - pvc.yaml
  - configmap.yaml
  - service.yaml
  - ingressroute.yaml
  # Scale-to-zero scaler
  - scaler-deployment.yaml
  - scaler-secrets.yaml
  # Local ollama on talos06 (always-on fallback)
  - ollama-talos06.yaml
  # UI applications
  - open-webui.yaml
  - sillytavern.yaml
  # - searxng.yaml  # Uncomment if needed
