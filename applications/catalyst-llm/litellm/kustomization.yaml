apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - external-secret.yaml
  - configmap.yaml
  - postgresql.yaml
  # TODO: Build and push litellm-pgvector image to local registry, then enable
  # - pgvector-service.yaml
  - deployment.yaml
  - service.yaml
  - ingressroute.yaml
