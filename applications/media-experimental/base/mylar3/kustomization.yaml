apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# mylar3 = ../_instance skeleton + deltas only (see patch-*.yaml).

resources:
  - ../_instance

labels:
  # App identity — stamped into metadata + selectors + pod template.
  - pairs:
      app: mylar3
    includeSelectors: true
  - pairs:
      app.kubernetes.io/component: comic-manager
      category: comic-manager

patches:
  - path: patch-deployment.yaml
    target: {kind: Deployment}
  - path: patch-ingressroute.yaml
    target: {kind: IngressRoute}
  # Rename template placeholders + set ports.
  - target: {kind: Deployment, name: app-template}
    patch: |-
      - {op: replace, path: /metadata/name, value: mylar3}
      - {op: replace, path: /spec/template/spec/containers/0/name, value: mylar3}
      - {op: replace, path: /spec/template/spec/containers/0/ports/0/containerPort, value: 8090}
  - target: {kind: Service, name: app-template}
    patch: |-
      - {op: replace, path: /metadata/name, value: mylar3}
      - {op: replace, path: /spec/ports/0/port, value: 8090}
  - target: {kind: IngressRoute, name: app-template}
    patch: |-
      - {op: replace, path: /metadata/name, value: mylar3}
  - target: {kind: PersistentVolumeClaim, name: app-template-config}
    patch: |-
      - {op: replace, path: /metadata/name, value: mylar3-config}
