apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# bindery = ../_instance skeleton + deltas only (see patch-*.yaml).

resources:
  - ../_instance
  - ingressroute-http.yaml  # web→https redirect (SSO route is on websecure)

labels:
  # App identity — stamped into metadata + selectors + pod template.
  - pairs:
      app: bindery
    includeSelectors: true
  - pairs:
      app.kubernetes.io/component: book-manager
      category: book-manager

patches:
  - path: patch-deployment.yaml
    target: {kind: Deployment}
  - path: patch-ingressroute.yaml
    target: {kind: IngressRoute, name: app-template}
  # Rename template placeholders + set ports.
  - target: {kind: Deployment, name: app-template}
    patch: |-
      - {op: replace, path: /metadata/name, value: bindery}
      - {op: replace, path: /spec/template/spec/containers/0/name, value: bindery}
      - {op: replace, path: /spec/template/spec/containers/0/ports/0/containerPort, value: 8787}
  - target: {kind: Service, name: app-template}
    patch: |-
      - {op: replace, path: /metadata/name, value: bindery}
      - {op: replace, path: /spec/ports/0/port, value: 8787}
  - target: {kind: IngressRoute, name: app-template}
    patch: |-
      - {op: replace, path: /metadata/name, value: bindery}
  - target: {kind: PersistentVolumeClaim, name: app-template-config}
    patch: |-
      - {op: replace, path: /metadata/name, value: bindery-config}
