---
apiVersion: v1
kind: Service
metadata:
  name: booksonic
  labels:
    app: booksonic
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 4040
      targetPort: http
      protocol: TCP
  selector:
    app: booksonic
