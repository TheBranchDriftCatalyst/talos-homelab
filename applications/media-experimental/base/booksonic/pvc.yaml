---
# Appdata on local-path (SQLite-safe). Node-pinned via deployment nodeAffinity.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: booksonic-config
  labels:
    app: booksonic
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
