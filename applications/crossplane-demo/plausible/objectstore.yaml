---
# Barman Cloud plugin ObjectStore for plausible-db — the former
# Cluster.spec.backup.barmanObjectStore stanza moved verbatim (same bucket/
# prefix/creds — WAL history and base backups carry over). retentionPolicy
# moves here too (was Cluster.spec.backup.retentionPolicy).
apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: plausible-db
  namespace: crossplane-demo
  annotations:
    # See postgres.yaml -- keep the live ObjectStore while the composite takes it over.
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  retentionPolicy: "30d" # prune backups + WALs older than 30 days
  configuration:
    destinationPath: s3://cnpg-backups/plausible-db
    endpointURL: http://minio-hl.minio.svc:9000
    s3Credentials:
      accessKeyId:
        name: cnpg-minio-backup
        key: ACCESS_KEY_ID
      secretAccessKey:
        name: cnpg-minio-backup
        key: ACCESS_SECRET_KEY
    wal:
      compression: gzip
    data:
      compression: gzip
