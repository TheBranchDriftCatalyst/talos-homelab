---
# The crossplane Object + ProviderConfig — applied by a SEPARATE Flux Kustomization
# (crossplane-demo-object) that dependsOn crossplane-demo, so provider-kubernetes has installed
# its kubernetes.crossplane.io CRDs first. TALOS-ja5.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - crossplane.yaml
