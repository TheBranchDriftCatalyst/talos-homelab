---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: scratch

resources:
  # namespace.yaml removed - managed by infrastructure/base/namespaces/
  # example-mongodb temporarily removed during community-operator → mongodb-kubernetes
  # (MCK) operator swap — re-added after the new operator is Running (TALOS-zadf.57)
  # - example-mongodb/
  # dbgate moved to infrastructure/base/databases/dbgate (databases ns) — TALOS-zadf
  - frigate/
  - scrypted/
