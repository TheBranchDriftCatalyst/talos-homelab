---
apiVersion: v1
kind: Service
metadata:
  name: immich
  labels:
    app: immich
    app.kubernetes.io/name: immich
    app.kubernetes.io/part-of: vpn-service
spec:
  selector:
    app: immich
  ports:
    - name: http
      port: 2283
      targetPort: 2283
      protocol: TCP
    - name: gluetun-control
      port: 8000
      targetPort: 8000
      protocol: TCP
