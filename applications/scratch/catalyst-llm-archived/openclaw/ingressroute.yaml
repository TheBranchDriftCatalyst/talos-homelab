---
# HTTPS access (uses *.talos00 wildcard cert from traefik namespace)
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: openclaw
  labels:
    app: openclaw
    app.kubernetes.io/name: openclaw
    app.kubernetes.io/part-of: catalyst-llm
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`openclaw.talos00`)
      services:
        - name: openclaw
          namespace: catalyst-llm
          port: 18789
  tls:
    secretName: talos00-wildcard-tls
---
# HTTP redirect to HTTPS
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: openclaw-redirect
  labels:
    app: openclaw
    app.kubernetes.io/name: openclaw
    app.kubernetes.io/part-of: catalyst-llm
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`openclaw.talos00`)
      middlewares:
        - name: redirect-https
          namespace: traefik
      services:
        - name: openclaw
          namespace: catalyst-llm
          port: 18789
