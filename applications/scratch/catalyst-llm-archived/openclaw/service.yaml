---
apiVersion: v1
kind: Service
metadata:
  name: openclaw
  labels:
    app: openclaw
    app.kubernetes.io/name: openclaw
    app.kubernetes.io/part-of: catalyst-llm
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 18789
      targetPort: http
      protocol: TCP
  selector:
    app: openclaw
