---
apiVersion: v1
kind: Service
metadata:
  name: openclaw
  labels:
    app: openclaw
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 18789
      targetPort: http
      protocol: TCP
  selector:
    app: openclaw
