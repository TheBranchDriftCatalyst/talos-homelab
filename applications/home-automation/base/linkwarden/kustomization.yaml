apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - external-secret.yaml
  - postgres-appdb.yaml # CatalystCNPGAppDB → Cluster + ObjectStore + ScheduledBackup
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingressroute.yaml
