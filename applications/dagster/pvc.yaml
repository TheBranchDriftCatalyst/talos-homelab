---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dagster-home
  labels:
    app: dagster
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: fatboy-nfs-appdata
  resources:
    requests:
      storage: 10Gi
