apiVersion: v1
kind: Secret
metadata:
  name: catalyst-dns-sync-secret
  namespace: dns
  labels:
    app: catalyst-dns-sync
type: Opaque
stringData:
  TECHNITIUM_PASSWORD: "admin"  # Change this to match Technitium password!
