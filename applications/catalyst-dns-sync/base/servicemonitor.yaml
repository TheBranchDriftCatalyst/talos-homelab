apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: catalyst-dns-sync
  namespace: dns
  labels:
    app: catalyst-dns-sync
spec:
  selector:
    matchLabels:
      app: catalyst-dns-sync
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics
