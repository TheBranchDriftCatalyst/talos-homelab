---
# Dedicated namespace for Zipline (moved off media-private, which is reserved for
# things deployed from the talos-private repo). TALOS-0w6d.
apiVersion: v1
kind: Namespace
metadata:
  name: zipline
  labels:
    app: zipline
