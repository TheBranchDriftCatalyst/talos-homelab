---
apiVersion: v1
kind: Service
metadata:
  name: zipline
  labels:
    app: zipline
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
  selector:
    app: zipline
