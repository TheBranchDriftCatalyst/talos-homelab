---
apiVersion: v1
kind: Secret
metadata:
  name: pterodactyl-panel
  labels:
    app: pterodactyl-panel
type: Opaque
stringData:
  # Generate with: head -c 32 /dev/urandom | base64
  APP_KEY: "base64:changeme_generate_a_real_key_here"
  DB_PASSWORD: "pterodactyl-changeme"
  # Hashids salt for encoding IDs
  HASHIDS_SALT: "changeme-random-salt"
