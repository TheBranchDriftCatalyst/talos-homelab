---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pterodactyl-panel
  labels:
    app: pterodactyl-panel
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
