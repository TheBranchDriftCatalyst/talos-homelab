---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pterodactyl-mariadb
  labels:
    app: pterodactyl-mariadb
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
