---
# TODO: Move these secrets to 1Password and use External Secrets Operator
# 1. Create a 1Password vault entry "pterodactyl-mariadb"
# 2. Add keys: MYSQL_ROOT_PASSWORD, MYSQL_PASSWORD
# 3. Replace this file with an ExternalSecret referencing 1Password
# 4. Rotate credentials after migration
apiVersion: v1
kind: Secret
metadata:
  name: pterodactyl-mariadb
  labels:
    app: pterodactyl-mariadb
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: "pterodactyl-root-changeme"
  MYSQL_DATABASE: "panel"
  MYSQL_USER: "pterodactyl"
  MYSQL_PASSWORD: "pterodactyl-changeme"
