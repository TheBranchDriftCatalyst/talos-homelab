---
# TODO: Move these secrets to 1Password and use External Secrets Operator
# 1. Create a 1Password vault entry "pterodactyl-kuber"
# 2. Add key: KUBER_TOKEN (generate in Panel admin UI under Nodes)
# 3. Replace this file with an ExternalSecret referencing 1Password
# 4. Rotate credentials after migration
apiVersion: v1
kind: Secret
metadata:
  name: pterodactyl-kuber
  labels:
    app: pterodactyl-kuber
type: Opaque
stringData:
  # Token for authenticating with the Panel
  # Generate this in the Panel admin UI under Nodes
  KUBER_TOKEN: "changeme-generate-in-panel"
