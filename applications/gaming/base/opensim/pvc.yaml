---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opensim-data
  labels:
    app: opensim
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 20Gi
