---
# Data PVC - NFS storage for Pulsarr persistent data
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pulsarr-data
  labels:
    app: pulsarr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: fatboy-nfs-appdata
  resources:
    requests:
      storage: 1Gi
