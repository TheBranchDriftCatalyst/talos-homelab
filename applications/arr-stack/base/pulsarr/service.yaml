---
apiVersion: v1
kind: Service
metadata:
  name: pulsarr
  labels:
    app: pulsarr
    app.kubernetes.io/name: pulsarr
    app.kubernetes.io/part-of: arr-stack
spec:
  type: ClusterIP
  ports:
    - port: 3003
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: pulsarr
