---
# Config PVC - NFS storage for Libation
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: libation-config
  labels:
    app: libation
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: fatboy-nfs-appdata
  resources:
    requests:
      storage: 5Gi
