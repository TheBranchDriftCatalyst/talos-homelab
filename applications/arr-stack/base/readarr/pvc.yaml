---
# Config PVC (SQLite database - uses local-path)
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: readarr-config
  labels:
    app: readarr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
