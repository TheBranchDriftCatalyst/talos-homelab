---
apiVersion: v1
kind: Service
metadata:
  name: stackarr
  labels:
    app: stackarr
    app.kubernetes.io/name: stackarr
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 7777
      targetPort: http
      protocol: TCP
  selector:
    app: stackarr
