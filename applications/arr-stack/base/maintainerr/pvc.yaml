---
# Data PVC - NFS storage for Maintainerr
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: maintainerr-data
  labels:
    app: maintainerr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: fatboy-nfs-appdata
  resources:
    requests:
      storage: 1Gi
