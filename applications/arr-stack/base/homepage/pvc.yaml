---
# Config PVC for homepage configuration
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homepage-config
  labels:
    app: homepage
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
