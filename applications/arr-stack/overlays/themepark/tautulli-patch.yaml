---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tautulli
spec:
  template:
    spec:
      containers:
        - name: tautulli
          env:
            - name: DOCKER_MODS
              value: 'ghcr.io/themepark-dev/theme.park:tautulli'
          envFrom:
            - configMapRef:
                name: themepark-env
