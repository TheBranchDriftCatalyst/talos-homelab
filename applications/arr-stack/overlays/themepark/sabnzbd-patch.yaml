---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sabnzbd
spec:
  template:
    spec:
      containers:
        - name: sabnzbd
          env:
            - name: DOCKER_MODS
              value: 'ghcr.io/themepark-dev/theme.park:sabnzbd'
          envFrom:
            - configMapRef:
                name: arr-common-env
            - configMapRef:
                name: themepark-env
