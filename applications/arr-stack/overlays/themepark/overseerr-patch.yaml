---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: overseerr
spec:
  template:
    spec:
      containers:
        - name: overseerr
          env:
            - name: DOCKER_MODS
              value: 'ghcr.io/themepark-dev/theme.park:overseerr'
          envFrom:
            - configMapRef:
                name: themepark-env
