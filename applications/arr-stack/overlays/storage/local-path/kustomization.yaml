apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - storage.yaml

# Local-path storage configuration (ReadWriteOnce)
# Uses Rancher local-path provisioner for local node storage
