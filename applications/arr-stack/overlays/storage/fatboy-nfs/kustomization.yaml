apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - storage.yaml

# NFS storage configuration (ReadWriteMany)
# Uses nfs-synology StorageClass for network-attached storage
