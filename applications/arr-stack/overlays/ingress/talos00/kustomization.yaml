apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ingressroutes.yaml

# Default IngressRoutes for talos00 domain
# All services accessible via <app>.talos00 (requires /etc/hosts entry)
