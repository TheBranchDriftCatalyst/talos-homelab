---
# Per-instance host allow-list (homepage rejects requests for other Hosts).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: homepage
spec:
  template:
    spec:
      containers:
        - name: homepage
          env:
            - name: HOMEPAGE_ALLOWED_HOSTS
              value: 'home.homepage.talos00'
