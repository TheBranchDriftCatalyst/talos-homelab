---
# Replaces the ENTIRE settings.yaml key of homepage-settings (kustomize
# cannot merge inside a string value). instanceName is this board's identity:
# only services whose IngressRoutes carry the
# `gethomepage.dev/instance.home` annotation appear on this board.
# Appearance lines mirror base/instance/configmap-settings.yaml — keep in sync.
apiVersion: v1
kind: ConfigMap
metadata:
  name: homepage-settings
data:
  settings.yaml: |
    ---
    # Homepage Settings — home board
    # Documentation: https://gethomepage.dev/configs/settings/
    instanceName: home
    title: Home — Talos Homelab
    description: Home automation & household tools
    favicon: https://raw.githubusercontent.com/walkxcode/dashboard-icons/main/png/kubernetes.png
    theme: dark
    color: slate
    cardBlur: sm
    headerStyle: boxed
    iconStyle: theme
    showStats: true
    statusStyle: dot
    hideVersion: false
    disableUpdateCheck: false
    target: _blank
    quicklaunch:
      searchDescriptions: true
      hideInternetSearch: false
      showSearchSuggestions: true
      provider: duckduckgo
    language: en
