# Phase 2c (TALOS-82um): ephemeral KubeVirt VM detonation sandbox — a REAL Debian shell where captured
# malware / escalated attacker sessions can actually execute (defeats every emulation-detection), fully
# caged. INTERNAL-ONLY for now (no attacker routing, no WAN). Privileged PSA — virt-launcher needs it.
apiVersion: v1
kind: Namespace
metadata:
  name: honeypot-vm
  labels:
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/audit: privileged
    pod-security.kubernetes.io/warn: privileged
