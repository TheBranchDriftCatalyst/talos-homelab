---
apiVersion: v1
kind: Service
metadata:
  name: zot
  namespace: registry
  labels:
    app.kubernetes.io/name: zot
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: zot
  ports:
    - name: http
      port: 5000
      targetPort: http
