---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tpot

resources:
  - namespace.yaml
  - cowrie.yaml
  # honeypot Deployments + LoadBalancer Services + CNP + log shipping added in the native build
