---
# GitRepository source for hubble-observer Helm chart
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: hubble-observer
  namespace: kube-system
spec:
  interval: 1h
  url: https://github.com/onzack/hubble-observer
  ref:
    branch: main
