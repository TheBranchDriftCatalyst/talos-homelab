{
  "name": "cilium-lbipam-dr-test",
  "version": "1.0.0",
  "private": true,
  "description": "Cilium LB-IPAM + L2 announcement disaster-recovery / chaos test (Jest). Measures LoadBalancer VIP L2 failover (lease re-election / ARP-owner move) on a throwaway canary. Tracks TALOS-23l.3.",
  "scripts": {
    "test": "jest --runInBand",
    "test:dr": "CILIUM_LBIPAM_DR_DESTRUCTIVE=1 jest --runInBand"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
