# Carrierarr Fleet Management
# This deploys the control-plane for managing worker agents
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - control-plane.yaml
  - ingressroute.yaml

commonLabels:
  app.kubernetes.io/part-of: carrierarr
