{
  "name": "honeypot-security-test",
  "version": "1.0.0",
  "private": true,
  "description": "Cowrie honeypot security-posture assertions (Jest). Read-only. Guards the controls that make deliberate internet exposure survivable. Tracks TALOS-hg7.",
  "scripts": {
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
