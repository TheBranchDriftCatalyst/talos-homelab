---
# Namespace for the RabbitMQ Cluster Operator + Messaging Topology Operator.
# Plain namespace — the Bitnami operators run as non-root/hardened, so no
# special Pod Security Admission level is required (baseline/restricted OK).
apiVersion: v1
kind: Namespace
metadata:
  name: rabbitmq-system
