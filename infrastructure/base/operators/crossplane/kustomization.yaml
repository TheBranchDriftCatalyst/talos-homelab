---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - helmrelease.yaml
  # provider.yaml + provider-rbac.yaml MOVED to applications/crossplane-demo/crossplane-provider.yaml
  # (they need pkg.crossplane.io CRDs that only exist after this HelmRelease reconciles, so they
  # must live in a Kustomization that dependsOn `operators`, not here — TALOS-ja5).
