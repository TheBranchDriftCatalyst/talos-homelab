---
# Namespace for KEDA (Kubernetes Event-Driven Autoscaling).
# Plain namespace — KEDA components run as non-root; no special Pod Security
# Admission level required.
apiVersion: v1
kind: Namespace
metadata:
  name: keda
