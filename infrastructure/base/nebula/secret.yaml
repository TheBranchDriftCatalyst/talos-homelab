---
# Nebula Lighthouse Certificates
# NOTE: This is a placeholder - actual certs should be created via:
#   kubectl create secret generic nebula-lighthouse-certs \
#     --from-file=ca.crt=configs/nebula-certs/ca.crt \
#     --from-file=lighthouse.crt=configs/nebula-certs/lighthouse.crt \
#     --from-file=lighthouse.key=configs/nebula-certs/lighthouse.key \
#     -n nebula
apiVersion: v1
kind: Secret
metadata:
  name: nebula-lighthouse-certs
  namespace: nebula
type: Opaque
stringData:
  # Placeholder - replace with actual certs or use kubectl create secret
  ca.crt: |
    REPLACE_WITH_CA_CRT
  lighthouse.crt: |
    REPLACE_WITH_LIGHTHOUSE_CRT
  lighthouse.key: |
    REPLACE_WITH_LIGHTHOUSE_KEY
