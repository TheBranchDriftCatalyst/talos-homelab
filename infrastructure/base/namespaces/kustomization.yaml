apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - external-dns.yaml
  - external-secrets.yaml
  - home-automation.yaml
  - media.yaml
  - media-experimental.yaml
  - monitoring.yaml
  - observability.yaml
  - registry.yaml  # re-owned here after Nexus decommission (TALOS-6vf); was previously created by the deleted registry Kustomization
  - forgejo.yaml  # Forgejo package registry (TALOS-6nf)
  - scratch.yaml
  - crossplane-demo.yaml # operator-catalog demo (TALOS-ja5)
  - tdarr.yaml
