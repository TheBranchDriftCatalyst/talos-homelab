{
  "name": "traefik-dr-test",
  "version": "1.0.0",
  "private": true,
  "description": "Traefik ingress disaster-recovery / chaos test (Jest). Measures ingress downtime when the serving Traefik pod is killed (single-replica DaemonSet/hostPort SPOF). Tracks TALOS-23l.8.",
  "scripts": {
    "test": "jest --runInBand",
    "test:dr": "TRAEFIK_DR_DESTRUCTIVE=1 jest --runInBand"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
