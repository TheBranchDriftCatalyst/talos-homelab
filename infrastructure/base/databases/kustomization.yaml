---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - cloudnative-pg/
  # mongodb-operator temporarily unreferenced while Flux uninstalls the old
  # community-operator HelmRelease before the mongodb-kubernetes (MCK) swap.
  # Re-added same commit-series with the MCK HelmRelease (TALOS-zadf.57)
  # - mongodb-operator/
  - minio-operator/
  - dbgate/ # DB GUI, promoted from scratch (TALOS-zadf)
  - dbgate-connection-sync/ # single-writer CronJob → dbgate-cnpg-connections Secret (TALOS-7jxm)
