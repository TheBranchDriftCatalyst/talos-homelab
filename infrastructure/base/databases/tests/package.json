{
  "name": "cnpg-dr-test",
  "version": "1.0.0",
  "private": true,
  "description": "CloudNativePG failover disaster-recovery / chaos test (Jest). Read-only checks assert the CNPG operator + prod clusters are healthy; the destructive tier spins up a throwaway canary cluster, kills its primary, and measures replica-promotion time. Tracks TALOS-23l.5.",
  "scripts": {
    "test": "jest --runInBand",
    "test:dr": "CNPG_DR_DESTRUCTIVE=1 jest --runInBand"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
