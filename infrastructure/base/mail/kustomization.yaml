apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mail

resources:
  - namespace.yaml
  - externalsecret.yaml
  # stalwart.yaml, service.yaml, ingressroute.yaml, networkpolicy.yaml — added next increment
