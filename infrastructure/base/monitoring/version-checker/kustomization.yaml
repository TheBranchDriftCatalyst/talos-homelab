---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Deploy into the existing `monitoring` namespace (no dedicated ns created).
namespace: monitoring
resources:
  - rbac.yaml
  - deployment.yaml
  - service.yaml
  - servicemonitor.yaml
