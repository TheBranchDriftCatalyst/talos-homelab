---
# GrafanaDashboard CRDs for Kubernetes cluster monitoring
# Migrated from scripts/import-grafana-dashboards.sh

# Kubernetes Cluster Monitoring (classic)
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: k8s-cluster-monitoring
  namespace: monitoring
  labels:
    app.kubernetes.io/component: dashboard
    dashboard-category: kubernetes
spec:
  folder: "Kubernetes"
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  grafanaCom:
    id: 315
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "Prometheus"
---
# K8S Dashboard (comprehensive 2025)
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: k8s-comprehensive
  namespace: monitoring
  labels:
    app.kubernetes.io/component: dashboard
    dashboard-category: kubernetes
spec:
  folder: "Kubernetes"
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  grafanaCom:
    id: 15661
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "Prometheus"
---
# Kubernetes / Views / Pods
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: k8s-pods-view
  namespace: monitoring
  labels:
    app.kubernetes.io/component: dashboard
    dashboard-category: kubernetes
spec:
  folder: "Kubernetes"
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  grafanaCom:
    id: 15760
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "Prometheus"
---
# Kubernetes Monitoring Overview
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: k8s-monitoring-overview
  namespace: monitoring
  labels:
    app.kubernetes.io/component: dashboard
    dashboard-category: kubernetes
spec:
  folder: "Kubernetes"
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  grafanaCom:
    id: 14623
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "Prometheus"
---
# Kubernetes PVC Dashboard
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: k8s-pvc
  namespace: monitoring
  labels:
    app.kubernetes.io/component: dashboard
    dashboard-category: kubernetes
spec:
  folder: "Kubernetes"
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  grafanaCom:
    id: 13646
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "Prometheus"
---
# Kubernetes Volumes Dashboard
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: k8s-volumes
  namespace: monitoring
  labels:
    app.kubernetes.io/component: dashboard
    dashboard-category: kubernetes
spec:
  folder: "Kubernetes"
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  grafanaCom:
    id: 11454
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "Prometheus"
