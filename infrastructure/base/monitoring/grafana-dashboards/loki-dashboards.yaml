---
# Loki Logging Dashboards for Kubernetes
# Provides log exploration and analysis via Loki datasource

# Loki Kubernetes Logs Dashboard (ID: 15141)
# Comprehensive log viewer for Kubernetes workloads
# Requires: Loki datasource
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: loki-kubernetes-logs
  namespace: monitoring
spec:
  folder: "Logging"
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  grafanaCom:
    id: 15141
  datasources:
    - inputName: "DS_LOKI"
      datasourceName: "Loki"
---
# Loki Stack Monitoring Dashboard (ID: 14055)
# Monitor Loki itself - ingestion rates, query performance
# Requires: Prometheus + Loki datasources
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: loki-stack-monitoring
  namespace: monitoring
spec:
  folder: "Logging"
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  grafanaCom:
    id: 14055
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "Prometheus"
    - inputName: "DS_LOKI"
      datasourceName: "Loki"
---
# Loki & Promtail Dashboard (ID: 10880)
# Overview of log pipeline health
# Requires: Prometheus datasource
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: loki-promtail
  namespace: monitoring
spec:
  folder: "Logging"
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  grafanaCom:
    id: 10880
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "Prometheus"
---
# Fluent Bit Dashboard (ID: 7752)
# Monitor Fluent Bit log shipping metrics
# Requires: Prometheus datasource
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: fluent-bit
  namespace: monitoring
spec:
  folder: "Logging"
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  grafanaCom:
    id: 7752
  datasources:
    - inputName: "DS_PROMETHEUS"
      datasourceName: "Prometheus"
---
# Loki Logs Quick Search (ID: 12019)
# Quick namespace/pod log browser
# Requires: Loki + Prometheus datasources
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: loki-logs-quick
  namespace: monitoring
spec:
  folder: "Logging"
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  grafanaCom:
    id: 12019
  datasources:
    - inputName: "DS_LOKI"
      datasourceName: "Loki"
    - inputName: "DS_PROMETHEUS"
      datasourceName: "Prometheus"
