---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring

resources:
  # tempo-s3-credentials (root) retired → scoped tempo-s3-scoped from minio/scoped-users (TALOS-xgrl.12)
  - helmrelease.yaml
