---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
resources:
  - helmrelease.yaml
  - rbac.yaml
  # Publishes /catalyst-rum/ on every host so the browser RUM beacon is
  # same-origin with the page emitting it (TALOS-xfjl.10).
  - faro-route.yaml
