---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
resources:
  # loki-s3-credentials (root) retired → loki now uses the scoped loki-s3-scoped
  # secret reflected from minio/scoped-users (TALOS-xgrl.12)
  - helmrelease.yaml
