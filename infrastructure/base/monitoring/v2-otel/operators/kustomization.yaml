---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring

resources:
  - otel-operator/
  # tempo-operator/ REMOVED (TALOS-slbn follow-up): unused — 0 TempoStack/TempoMonolithic
  # CRs exist; Tempo runs from the Helm chart (tempo-0 StatefulSet). The operator only crash-looped
  # on lost leader election (18 restarts) while reconciling nothing, and shipped Fail-policy admission
  # webhooks (a latent cluster-wide pod-admission hazard when it's down).
  - clickstack-mongodb-secret.yaml # mongo cred; must exist BEFORE monitoring postBuild renders
