---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - helmrelease.yaml
  # grafana-instance.yaml is applied via separate grafana-instances kustomization
  # that depends on the operator being installed first (CRD dependency)
