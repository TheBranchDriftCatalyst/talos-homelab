{
  "name": "minio-dr-test",
  "version": "1.0.0",
  "private": true,
  "description": "MinIO tenant disaster-recovery / chaos test (Jest). Verifies the single-server NFS-backed S3 backend (Loki/Mimir/Tempo/Velero) survives a pod kill with byte-identical data + measures recovery wall-time. Tracks TALOS-23l.2.",
  "scripts": {
    "test": "jest --runInBand",
    "test:dr": "MINIO_DR_DESTRUCTIVE=1 jest --runInBand"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
