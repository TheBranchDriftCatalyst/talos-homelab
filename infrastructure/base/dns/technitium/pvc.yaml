apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: technitium-dns-config
  namespace: dns
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
