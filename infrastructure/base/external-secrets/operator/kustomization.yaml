apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Note: namespace.yaml is deployed by the parent kustomization or Flux
# This kustomization only handles the Helm chart resources
resources:
  - helmrepository.yaml
  - helmrelease.yaml
