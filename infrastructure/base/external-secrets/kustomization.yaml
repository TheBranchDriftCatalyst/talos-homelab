apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Note: namespace.yaml and operator are deployed via external-secrets-operator Flux Kustomization
# This kustomization only deploys resources that depend on the operator CRDs
resources:
  - onepassword-connect
  - secretstores
  - jobs
