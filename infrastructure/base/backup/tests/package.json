{
  "name": "velero-dr-test",
  "version": "1.0.0",
  "private": true,
  "description": "Velero backup / disaster-recovery chaos test (Jest). Read-only asserts the DR machinery is healthy; the destructive tier does a real backup→delete→restore round-trip against a throwaway canary namespace and proves zero data loss via sha256. Tracks TALOS-23l.1.",
  "scripts": {
    "test": "jest --runInBand",
    "test:dr": "VELERO_DR_DESTRUCTIVE=1 jest --runInBand"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
