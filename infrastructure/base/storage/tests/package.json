{
  "name": "nfs-lifecycle-dr-test",
  "version": "1.0.0",
  "private": true,
  "description": "NFS/PVC lifecycle disaster-recovery / chaos test (Jest). Guards reclaim/reuse data-hygiene on fatboy-nfs-appdata + local-path after the 2026-05-09 total local-path PVC loss. Tracks TALOS-23l.7.",
  "scripts": {
    "test": "jest --runInBand",
    "test:dr": "NFS_DR_DESTRUCTIVE=1 jest --runInBand"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
