---
# Secret is now managed by ExternalSecret (discord-webhook-externalsecret.yaml)
# The webhook URL is synced from 1Password vault item: flux-discord-webhook
---
apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Provider
metadata:
  name: discord
  namespace: flux-system
spec:
  type: discord
  username: 'Flux Bot'
  channel: 'talos00'
  secretRef:
    name: discord-webhook
