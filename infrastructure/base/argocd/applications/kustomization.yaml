apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# ArgoCD Application manifests
# These are auto-applied by Flux when pushed to main
# ArgoCD then watches the target repos and auto-syncs
resources:
  - catalyst-ui.yaml
  - arr-stack-private.yaml
  - kasa-exporter.yaml
