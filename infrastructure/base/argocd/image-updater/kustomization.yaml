---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - zot-credentials.yaml  # Zot pull creds for the updater (see the file)
  - install.yaml
  - catalyst-ui-image-updater.yaml
  - catalyst-data-image-updater.yaml
  - boomtime-image-updater.yaml
  - fchat-bouncer-image-updater.yaml

patches:
  - path: configmap-patch.yaml
