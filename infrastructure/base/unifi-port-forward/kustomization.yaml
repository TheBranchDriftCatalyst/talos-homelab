---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: unifi-port-forward
resources:
  - namespace.yaml
  - crd.yaml
  - serviceaccount.yaml
  - rbac.yaml
  - rbac-crd-read.yaml
  - externalsecret.yaml
  - deployment.yaml
  # Verified against the live UDM table + gaming/* NodePort Services (2026-09-12) and wired in.
  # Adopts each live forward by WAN-port match. RDP omitted (removed from WAN — see the file).
  - portforward-rules.yaml
