---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: unifi-port-forward
resources:
  - namespace.yaml
  - crd.yaml
  - serviceaccount.yaml
  - rbac.yaml
  - externalsecret.yaml
  - deployment.yaml
  # portforward-rules.yaml is DELIBERATELY NOT wired in yet. Those CRs adopt LIVE
  # forwards on first sync (the controller takes ownership by matching WAN port).
  # They were transcribed from the UI and MUST be verified against the real table
  # before they can mutate the edge. Review, correct, then add the line below.
  # - portforward-rules.yaml
