{
  "name": "authentik-dr-test",
  "version": "1.0.0",
  "private": true,
  "description": "Authentik SSO disaster-recovery / chaos test (Jest). Read-only checks assert the auth SPOF's DR machinery exists + is healthy; destructive scenario kills authentik-server and MEASURES OIDC downtime. Tracks TALOS-23l.4.",
  "scripts": {
    "test": "jest --runInBand",
    "test:dr": "AUTHENTIK_DR_DESTRUCTIVE=1 jest --runInBand"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
