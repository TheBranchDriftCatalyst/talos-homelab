---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: mojo2600
  namespace: pihole
spec:
  interval: 24h
  url: https://mojo2600.github.io/pihole-kubernetes/
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pihole
  namespace: pihole
spec:
  interval: 30m
  timeout: 10m
  chart:
    spec:
      chart: pihole
      version: '2.38.0' # appVersion 2026.07.2 = Pi-hole v6
      sourceRef:
        kind: HelmRepository
        name: mojo2600
        namespace: pihole
      interval: 12h
  install:
    createNamespace: false
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    image:
      repository: pihole/pihole
      # tag inherits chart appVersion (Pi-hole v6)
    replicaCount: 1
    # Stateful (local-path PVC is node-local) → pin to a stable node.
    nodeSelector:
      kubernetes.io/hostname: talos03

    # Admin password from ESO (same value feeds external-dns-pihole).
    admin:
      enabled: true
      existingSecret: pihole-admin
      passwordKey: password

    # DNS service: dedicated LAN IP via Cilium LB IPAM (pool selects app: pihole).
    serviceDns:
      type: LoadBalancer
      mixedService: true # single Service carrying TCP+UDP :53
      annotations:
        lbipam.cilium.io/ips: "192.168.1.240"

    # Web/admin UI: dedicated LAN IP (.241) so it's reachable without DNS/hosts,
    # plus the IngressRoute. external-dns-pihole talks to this service's API.
    serviceWeb:
      type: LoadBalancer
      annotations:
        lbipam.cilium.io/ips: "192.168.1.241"
      http:
        enabled: true
        port: 80
      https:
        enabled: false

    persistentVolumeClaim:
      enabled: true
      storageClass: local-path
      size: 1Gi

    # Upstreams: rely on the chart defaults (FTLCONF_dns_upstreams=8.8.8.8;8.8.4.4,
    # FTLCONF_dns_listeningMode=all). Do NOT set FTLCONF_dns_upstreams here — the
    # chart already renders it, and a second entry is a duplicate-env error.
    # (Legacy dnsmasq.upstreamServers is also avoided — v6 rejects server= lines.)
    extraEnvVars:
      FTLCONF_dns_listeningMode: "all" # answer on the LB IP (belt-and-suspenders)
      # Conditional forwarding: send reverse (PTR) lookups for the LAN subnet to the
      # router so Pi-hole can show CLIENT HOSTNAMES instead of raw IPs in the graphs.
      # Format: <enabled>,<cidr>,<router-ip>#<port>,<local-domain>
      # Domain matches the router's DHCP "Domain Name" (localdomain).
      # ⚠ Adjust 192.168.1.1 if your gateway/DHCP-DNS is elsewhere.
      FTLCONF_dns_revServers: "true,192.168.1.0/24,192.168.1.1#53,localdomain"
