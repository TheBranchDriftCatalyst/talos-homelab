{
  "name": "pihole-dr-test",
  "version": "1.0.0",
  "private": true,
  "description": "Pi-hole HA disaster-recovery / chaos test (Jest). Measures DNS failover downtime under injected failures. Tracks TALOS-0nt.2.",
  "scripts": {
    "test": "jest --runInBand",
    "test:dr": "PIHOLE_DR_DESTRUCTIVE=1 jest --runInBand"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
