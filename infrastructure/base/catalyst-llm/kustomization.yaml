---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: catalyst-llm

resources:
  - namespace.yaml
  - pvc.yaml
  - configmap.yaml
  - service.yaml
  - ingressroute.yaml
  # Scale-to-zero scaler
  - scaler-deployment.yaml
  - scaler-secrets.yaml
  # UI applications (optional)
  # - open-webui.yaml
  # - sillytavern.yaml
  # - searxng.yaml
