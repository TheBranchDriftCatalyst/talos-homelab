# ShellCheck Configuration
# https://github.com/koalaman/shellcheck/wiki/Directive

# Only disable checks that are false positives or stylistic in this codebase

# SC1091: Not following sourced file (can't resolve at lint time)
disable=SC1091

# SC2034: Variable appears unused (used via sourcing)
disable=SC2034

# SC2155: Declare and assign separately (stylistic, not a bug)
disable=SC2155

# SC2016: Single quotes don't expand (intentional for sed/regex patterns)
disable=SC2016

# SC2059: Variables in printf format (used intentionally for colors)
disable=SC2059

# SC2162: read without -r (OK for interactive prompts with no backslashes)
disable=SC2162

# SC2005: Useless echo (sometimes used for clarity)
disable=SC2005
